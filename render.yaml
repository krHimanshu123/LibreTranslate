services:
  - type: web
    name: LibreTranslate
    env: python
    plan: starter
    region: oregon
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
    buildCommand: bash render-build.sh
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT wsgi:app
